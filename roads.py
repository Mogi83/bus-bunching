class Roads:
    def __init__(self):
        self.start = np.array([self.start_x, self.start_y])
        self.stop = np.array([self.end_x, self.end_y])
        #traffic lights?
        #curves?
        #stop signs?
        #traffic in general?